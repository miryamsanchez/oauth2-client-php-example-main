{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run local server",
            "type": "shell",
            "group": "test",
            "command": "php -d variables_order=EGPCS -S 0.0.0.0:8000",
            "options": {
                "cwd": "${workspaceFolder}",
                "env": {
                    "APP_DEBUG": "true",
                }   
            },
        }
    ]
}